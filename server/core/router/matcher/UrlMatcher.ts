import {RouteCollection} from "../RouteCollection";

export class UrlMatcher {

    public match() {}
    
    protected matchCollection(path: string, routes: RouteCollection) {
        new Map
    }

}